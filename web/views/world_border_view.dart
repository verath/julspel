part of julspel;


class WorldBorderView extends GameObjectView {
  WorldBorder _worldBorder;
  
  WorldBorderView( WorldBorder this._worldBorder);
  
  
  // TODO: Some border here would probably be nice.
  void render(ViewCanvasDraw canvasDraw, num timestamp) {
  }
}